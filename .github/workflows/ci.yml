name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  ci:
    uses: matt-riley/matt-riley-ci/.github/workflows/ci.yml@v2
    with:
      build: true
      test: true
      vet: true
      fmt: true
      lint: true
      task-prefix: go-
      concurrency-suffix: ci

  govulncheck:
    needs: ci
    if: ${{ always() }}
    uses: matt-riley/matt-riley-ci/.github/workflows/go-security.yml@v2
    with:
      go-version-file: cli/go.mod
      working-directory: cli
      concurrency-suffix: security
